<p style="color: green"><%= notice %></p>

<% content_for :title, "Banks" %>

<h1>Bancos</h1>

<div id="banks">
  <% @bancos.each do |bank| %>
    <%= render bank %>
    <p>
      <%= link_to "Show this bank", bank %>
    </p>
  <% end %>
</div>

<%= link_to "New bank", new_bank_path %>
